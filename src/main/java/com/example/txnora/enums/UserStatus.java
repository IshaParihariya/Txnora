package com.example.txnora.enums;

/**
 * user status
 * invite sent -> pending
 * accepted and pass done -> activated
 * and further deactivated
 */
public enum UserStatus {
    PENDING,
    ACTIVE,
    DISABLED
}

